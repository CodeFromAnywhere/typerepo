import { Device } from "peer-types";
/**
 * Only works if isOnlineCalculated is set (using augmentDevice)
 *
 * Sorts device: first favorite, then online, then offline
 */
export declare const sortDevices: (a: Device, b: Device) => 1 | -1;
//# sourceMappingURL=sortDevice.d.ts.map